# P0 视觉审查：图 1 第 3 版

状态：等待用户审查，未开始应用代码开发。

本次仅删除学习记录窗口日期下方的“北京时间”标签。计时、每日归档和零点分日仍固定使用 Asia/Shanghai；这是界面文案修改，不是时区规则修改。

沿用第 2 版：整小时备注卡片从计时窗上方弹出；记录中的学习时长使用中文单位，钟点保持 HH:MM，悬浮计时数字保持 HH:MM。

文件：`01-overall-light.png`；完整编辑提示词见 `prompts.md`。使用内置 image_gen 图片编辑，模型型号未由接口暴露。原版及前两版均保留。

已查看图片，确认标签已移除且其余布局及示例内容保留。图 2 仍为 `../v1/02-states-dark.png`，后续统一视觉规范时应同步中文时长格式及移除时区标签。

