# P0 视觉审查：图 1 第 2 版

状态：已按用户反馈修订，等待用户审查；未开始应用开发。

## 本次修改

1. 整小时备注卡片从完整悬浮计时窗的上方弹出，以向下的小尖角指向计时窗；备注输入和保存期间计时继续。图中是静态关键画面，实际弹出动画和焦点行为仍待开发验收。
2. 学习记录里的钟点保留 `09:00–10:00`、`10:20–至今`；学习时长改为“1小时”“1小时35分钟”“35分钟”。学习段摘要中的时长另用浅青绿色标签区分。
3. 记录窗口的每日累计及侧栏总时长同步采用中文单位；悬浮计时窗继续使用 `HH:MM`。

## 文件与范围

- `01-overall-light.png`：本次修订的图 1。
- `prompts.md`：本次编辑的完整提示词。
- 图 2 的上一版仍在 `../v1/02-states-dark.png`，本次未修改。后续确认视觉规范时，深色记录页也应使用本次统一的中文时长格式。
- 原始版本完整保留，没有覆盖。

## 已检查

卡片位于计时窗上方；计时窗数字及按钮仍完整可见；钟点和时长格式明确区分；记录示例的时长合计仍为 2小时35分钟；未增加业务功能。

