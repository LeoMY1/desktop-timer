# P2 v2 自测记录

日期：2026-09-18。**内部自测完成，用户尚未验收。**

## 环境与构建
- Apple Silicon，macOS 26.6.2 (25G83)，Swift 5.8.1，Command Line Tools / SDK 13.3。
- `bash scripts/build.sh release` 成功，arm64 / macOS 13 部署目标；生成可双击 .app，codesign --verify --strict 通过。
- `swift build` 在当前 CLT 环境报 SDK PlatformPath 查询错误；改用随源码交付的直接 swiftc 构建脚本，无第三方依赖。
- 正常启动曾发现 UserDefaults suite 初始化返回 nil 导致崩溃，已改为独立 P2 bundle 的 UserDefaults.standard；普通启动重新验证成功。

## 独立逻辑检查：7/7 通过
执行 `bash scripts/test.sh`。覆盖：上方有空间不移动、顶部仅必要下移、负坐标显示器布局、屏外位置恢复、左右边缘、较小可见区域、重复布局不漂移。

证据：`outputs/P2-v2/evidence/geometry-checks.json`。这是矩形布局检查，不是实际多显示器测试。

## Release AppKit 脚本检查：35/35 通过
对真实 NSPanel 实例执行 `--self-check`，报告 `outputs/P2-v2/evidence/appkit-self-check.json`。

覆盖初始状态、非激活与悬浮配置、全屏/多桌面标志、弹出不成为 key window/不改变前台应用、卡片在上、顶部下移、屏内布局、点击不取消复位、关闭复位、模拟拖动取消复位、草稿内存保留、输入区域尺寸、显式获取文本焦点、原生中文文本插入与选区替换、隐藏/恢复、暂停/继续和停止尾段。

这里的文本插入通过 NSTextView API 完成，不代表真实中文输入法候选词流程；模拟拖动指直接移动窗口并调用拖动生命周期，不代表指针拖动验收。全屏/Spaces 项仅验证配置，不能据此宣称实际覆盖已通过。

同一 Release 生成 12 张原生视图 PNG：浅色/深色下未开始、学习中、暂停、整小时卡片、尾段卡片和历史页。已检查主要页面的文字与布局。原生截图来自应用自己的内容视图，不包含其他应用内容。

## 实际界面操作
- 普通方式启动 .app 成功；真实点击开始→暂停→继续，状态与按钮按预期变化。
- 在文本编辑器的新建测试文稿中输入后触发 8 秒延迟卡片；卡片出现后读取编辑器状态，焦点仍是原文本输入区。
- 真实打开历史页、铅笔编辑，执行全选、中文粘贴、完成，历史行出现修改后的文字。
- 执行过真实指针拖动，但自动化提供的窗口截图不足以核实屏幕坐标变化及完整跟随路径，因此不标为拖动验收通过。
- 原生 UI 自动化期间出现过连接关闭/剪贴板读取超时；重连可读取原型。未据此把后续未测场景写为通过。

## 待用户实际验收
全屏浏览器与桌面切换覆盖、普通控制按钮与卡片的完整焦点往返、真实中文输入法候选框、指针拖动/顶端恢复/主动拖动后保留位置、菜单栏找回、系统实时外观切换。具体操作见 p2-testing.md。

多显示器插拔和其他 macOS 版本未测。没有执行真实睡眠/跨天/数据恢复测试：这些业务尚未开发，属于后续阶段。

## 交付边界
P2 已交付可测试的交互原型及源码；用户验收仍待确认。当前没有学习数据库，也没有真实累计或提醒逻辑。P3 未开始。

## v2 拖动修订
用户反馈 v1 的拖动范围太窄。v2 改为主窗全区域命中，并通过实际按钮布局排除按钮；手形光标同样避开按钮。新增 12 项原生视图命中检查（4 种状态下分别检查四边、数字/底部文字/边距、按钮排除），共 35/35 通过。复位、卡片和输入相关检查同时回归通过。几何算法未变，7 项结果沿用 v1。

真实界面坐标点击开始按钮成功；自动化拖动未观察到已保存坐标变化，标题区对照拖动也一样，因此不宣称已通过真实指针拖动验收。请用户在 v2 测试四周拖动与卡片跟随。
