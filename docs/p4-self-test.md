# P4 v1 自测记录

日期：2026-09-18。P4 开发与内部验证完成，等待用户验收。

## 环境与构建
当前 Apple Silicon Mac，Swift 5.8.1 / Command Line Tools，最低部署目标 macOS 13。`bash scripts/build-p4.sh release` 成功；本地 ad-hoc 签名校验通过。其他系统版本、机型及公开发行公证未验证。

## 46/46 核心与 SQLite 检查
命令：`bash scripts/test-p4.sh`。在 P3 的 26 项基础上加入 20 项 P4 检查，使用新隔离目录和可控时钟；报告 `evidence/core-checks.json`。

新增覆盖：25＋35 跨学习段达整小时、多小时漏回调补记录、阈值即时保存、暂停/重复刷新去重、已展示/未展示提醒重启行为、备注修改清空及重开、编辑期间继续计时、新小时不覆盖旧备注、90＋30 尾段合并不重复计时、单独尾段、禁止跨学习段合并、待处理先落盘、午夜/退出自动尾段、备注/合并/展示失败保留旧数据、SQLite 完整记录重开，以及用户确认的退出处理未选择尾段规则。

## 24/24 AppKit 集成检查
命令：`bash scripts/test-ui-p4.sh`。使用 Release 应用、隔离时钟、真实 SQLite；报告 `evidence/ui-checks.json`，截图展示计时窗、上方卡片、尾段和深浅色历史页。

检查卡片在上方且不主动成为键盘窗口、不改变前台应用、顶端避让/恢复、卡片期间持续计时、新提醒不替换当前条目与草稿、队列顺序、停止尾段选择、合并文字与总时长、无上一条禁合并、隐藏补多个小时、批次导航、已展示不重复、退出处理、完整快照重读。

截图检查发现过“合并结果被旧的已保存草稿缓存遮住”的问题。现已在成功保存后清除对应缓存，并增加“界面读取的合并文字与保存结果一致”检查；同时允许历史备注多行展开。修复后 24/24 通过。

## 实际原生界面验证
使用独立验收模式真实点击开始、通过菜单推进 60 分钟，主窗显示 01:00，历史页出现整小时记录。打开铅笔编辑，在原生输入框粘贴中文多行备注并完成，历史页展示两行文字。

通过菜单快捷键正常退出后，读取隔离 SQLite：备注为“P4验收：极限与连续\n补充复习错题”，状态 stopped，没有尚未处理的尾段。结果见 `evidence/native-ui.json`。此项验证中文文本输入和保存，不等同于完整拼音候选框流程已人工验收。

两个 `.command` 验收入口已实际启动验证：新建独立目录、正常退出、继续相同目录，重开保持未计时。见 `evidence/launcher-checks.json`。

## 用户确认与数据处理
用户报告真实睡眠、锁屏测试通过，并授权进入 P4；自然跨午夜没有实测条件，仍标为未实测。

用户确认旧记录都是测试，不需要保留到新版。P4 使用新的 `StudyTimerP4` 数据目录及独立 bundle ID，旧 P3 测试库不导入、不改写。独立验收模式也不写正式 P4 数据库。

## 未测与交付边界
真实跨午夜仍未执行；所有跨日和小时跳跃检查均明确为可控时钟。P4 本版的实际睡眠/锁屏、全屏/Spaces、完整中文输入法候选交互仍建议用户回归。窗口命中和焦点配置检查不能替代完整使用体验。

P5 最终回归及发布交付尚未开始。当前结果只作为 P4 交付证据，不代表用户已经验收。
