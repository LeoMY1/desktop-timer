# P5 最终回归与交付记录

日期：2026-09-18。用户已确认 P4 完成并授权 P5；本次开发、内部回归和最终打包完成，等待用户最终验收。

## 交付与环境
学习计时 1.0.0，arm64，最低部署目标 macOS 13。本机 macOS 26.6.2 / Apple Swift 5.8.1 / Command Line Tools。正式应用本地 ad-hoc 签名，未进行 Developer ID 公证。

`build-p5.sh release` 构建日常应用；`build-p5.sh test` 构建独立内部检查应用。正式版的菜单及标题无阶段号、演示外观或快进入口，旧验收参数在打开数据库前直接拒绝。应用包不包含验收启动器、检查应用、测试数据库或缓存。

为保留既有学习记录和窗口位置，bundle 标识、StudyTimerP4 数据目录及 SQLite/快照版本 2 保持不变。

## 本轮重新执行的检查
| 项目 | 结果 | 证据 |
|---|---|---|
| 核心计时、内容记录与 SQLite | 51/51 通过 | evidence/core-checks.json |
| 窗口几何布局 | 7/7 通过 | evidence/geometry-checks.json |
| AppKit 窗口、草稿、提醒、尾段、动画与命中 | 44/44 通过 | evidence/ui-checks.json |
| 真实时间与独立进程恢复 | 5/5 通过 | evidence/process-checks.json |
| 正式应用与测试入口分离 | 通过 | evidence/release-boundary.json |
| P4 既有数据副本兼容 | 通过 | evidence/upgrade-compatibility.json |
| 最终正式应用原生完整流程 | 通过 | evidence/native-final.json |
| 应用包解压、签名、归档完整性 | 通过 | evidence/package-checks.json |

核心用例包括 25＋35、90＋30、23:40 至 00:20、暂停跨日、睡眠间隔补算、异常恢复、损坏/未知库保留、存储失败、阈值去重、修改/清空备注、退出自动处理尾段及合并不重复计时。

44 项窗口检查包含真实托管视图中停止/运行/暂停三个状态的四边、数字和空白拖动命中，按钮排除及两个 20×20 点击范围；窗口层级、Spaces/全屏配置、卡片上方避让/恢复、不抢焦点、新提醒排队、备注合并、深浅色视图与 30 秒动画。动画通过真实主队列异步观测三次明暗周期、29 秒仍在及 30 秒结束。窗口配置检查不等于真实 Spaces/全屏操作已重新实测。

真实时间检查使用与正式应用相同的 Release StudyCore：运行 65 秒显示 00:01，暂停等待不累计，继续增加经过时间；正常重启不补算关闭间隔。测试进程主动 SIGKILL 后仅恢复最近成功检查点。所有写操作使用隔离测试库。

## 数据兼容和真实界面操作
通过 SQLite 只读备份将本机停止状态的 P4 库复制到隔离目录，使用正式版核心恢复副本。学习段、内容备注和里程碑均保留，核对原库快照未被修改。报告只记录结果，不打包用户数据库或备注。

对最终正式 `.app` 使用独立、预置 59 分 55 秒历史的测试库，以真实连续时钟启动。实际点击开始，菜单中无测试入口；经过 5 秒后触发整小时卡片。通过卡片原生文本框输入“P5最终验收：中文备注”及第二行“复习极限与连续”，保存后历史显示两行。

实际点击停止，历史出现待处理尾段；鼠标菜单退出后自动并入同段上一条。再次打开最终应用，保持停止，原备注和日累计仍在，已展示提醒不重复。退出间隔前后的学习段、条目和里程碑一致，无额外累计。这是预置隔离历史加真实时间交互验证，不声称本轮等待了完整一小时。

## 已知限制与用户验收
自然跨午夜仍未实际运行验证，分日自动化使用可控时钟；本轮没有物理睡眠/锁屏。此前用户报告睡眠/锁屏通过，以及明确确认 P2、P4 完成，保留为用户验收记录，不冒充本轮助手实测。

完整中文输入法候选框、全屏/Spaces、多显示器和其他 macOS/机型的最终日常体验仍建议按 p5-testing.md 复核。签名仅本地 ad-hoc，跨机器发行、公证不在本次完成范围。未新增云服务、同步、导出或手动修改时长。

最终源码与构建脚本、应用、使用说明、验收单及结果均已交付；用户最终验收尚待确认。
