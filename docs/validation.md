# 验证记录与验收边界

## 当前可重复运行的检查
`bash scripts/test.sh` 在隔离目录重建当前版本，依次执行：51 项核心与 SQLite、7 项几何、5 项真实时间/独立进程、44 项内部 AppKit 集成及发布边界检查。原始结果在 work/evidence，可重新生成；打包读取这些报告，不要求个人本机的人工文件。

完整运行需要 macOS 桌面会话，包含约 65 秒真实计时以及三十秒动效观察。窗口检查采用真实 AppKit 视图、可控学习时钟；不能据此宣称自然跨日、物理睡眠或所有全屏环境通过。

## P5 交付时已有的人工记录
历史交付在当前 Apple Silicon Mac 进行了最终应用实际操作：使用隔离预置 59 分 55 秒历史，真实时钟触发整小时卡片；输入并保存中文多行备注；停止形成尾段，鼠标退出自动合并；重开后记录和文字保持，未自动计时。预置历史不是等待完整一小时。

另通过只读备份对既有数据副本恢复，核对学习段、条目和提醒状态保留，原数据库未修改。该人工兼容检查是历史证据，当前仓库不包含个人数据库，也不要求他人具有相同记录才能打包。

用户明确验收过 P2 与 P4，并报告真实睡眠/锁屏通过。P5 后用户接受当前成果作为可运行原型，并授权开源整理；这不扩大为所有机型或自然跨午夜均已验证。

## 最终使用复核
- 启动保持停止，点击开始显示实际钟点；暂停不累计，停止后再次开始不清空今日总计。
- 四边和数字可拖动，两个右上角按钮可点周围空白；卡片上方避让与恢复正确。
- 整小时卡片不抢外部输入焦点，星光三次闪烁、三十秒停留；中文候选与多行备注需在自己的输入法环境检查。
- 隐藏不暂停，菜单栏能恢复；鼠标菜单退出保存，重新打开不补计关闭期间。
- 运行跨日保留昨日旧段，暂停跨日不启动；自然午夜需具备条件后实测。

## 限制
当前环境：Apple Silicon、macOS 26.6.2、Swift 5.8.1 / Command Line Tools，部署目标 macOS 13。未对其他 macOS/硬件全面验证，未做 Developer ID 公证。自然跨午夜仍未实测；本轮不改变系统时间、不执行物理睡眠。全屏/Spaces、多显示器与完整中文候选交互仍需用户按环境复核。

## 开源整理回归
2026-09-18：统一入口的本地完整回归通过，核心 51/51、几何 7/7、真实时间与进程 5/5、内部窗口 44/44、发布边界 5/5。公开的本轮报告位于 [evidence](evidence/)，不包含数据库或个人记录。

历史快照已核对工程清单、源码及构建入口，并对全部历史文本对象检查个人路径、凭据模式和不应公开的产物；未发现需阻止发布的内容。旧阶段没有在本轮重新构建。

发布后的远端克隆核验将在本地维护记录中保存，只有核验通过才清理原始阶段包。
