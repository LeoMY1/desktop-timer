# P4 v2 修复与自测

2026-09-18。范围仅为 P4 用户反馈修复，等待用户复测；P5 未开始。

## 修复与排查结论
1. 星光共停留 30 秒，出现后最初 2.4 秒内柔和闪烁 3 次，之后常亮；卡片仍由用户保存或收起，计时不中断。
2. 两处退出菜单均移除快捷键。请点击菜单退出；悬浮窗不抢键盘焦点，其他应用在前台时 Command-Q 仍属于其他应用。
3. 只读核对确认固定 09:00 和跨日大额累计来自独立验收模式；未发现模拟库覆盖正式库。公开版本不包含个人真实记录。
4. 交互验收现使用独立 PreviewTimeSource，从当前真实时间开始；连续经过时间独立于界面刷新，快进增加显式偏移。固定 9 点仅保留在自动化用例中。窗口明确显示模拟时刻和快进会计入测试时长，跨日操作显示目标与新增时长并要求点击确认。

旧模拟记录不会被猜测或转换为真实学习时间，v2 日常使用沿用已有正式 P4 库。新建独立验收使用新的测试目录。

## 核心与存储
`bash scripts/test-p4.sh`：51/51 通过。保留原 46 项，增加：真实开始时刻；验收时钟非固定 9 点且覆盖长间隔；恢复不回拨；已有学习记录与备注在跨日、保存和恢复后不变；暂停跨日不增加累计。

重点用例：先保存 18:17 开始的 30 分钟，再从 23:40 连续学到次日 00:20。昨日合计 50 分钟，今日 20 分钟；第一段的开始时间、时长和备注完全保留。

## 窗口与特效
`bash scripts/test-ui-p4.sh`：在原 24 项上增加两处退出菜单无快捷键、3 次明暗周期、29 秒仍显示、30 秒结束，共 28 项，最终 28/28 通过。特效检查通过真实主运行循环异步观测，计时核心仍使用隔离模拟时钟。

首次特效检查的嵌套等待阻塞了主队列回调，造成两项失败；已修正检查方式为异步采样，避免将测试阻塞误判为应用动画故障。最终原始结果见 `evidence/ui-checks.json`。

## 原生实际操作
以真实时钟、隔离数据库启动 Release 应用（不带验收参数），实际点击开始并打开历史，显示 20:48，与本次系统时间一致。通过应用菜单鼠标点击退出，进程正常结束，SQLite 中开始时刻落在本次启动/退出区间，状态 stopped、结束原因 quit。见 `evidence/native-real-time.json`。

本次没有使用 Command-Q 测试退出，也没有关闭其他应用。旧库只读排查见 `evidence/reported-time-diagnosis.json`。

## 验证边界
自然跨午夜、真实睡眠及锁屏未在此修复轮重做；跨日使用隔离时钟，不等同于自然午夜实测。动画的柔和程度及完整日常体验仍等待用户复测。

新版独立验收也完成实际操作：初始显示当前 20:50/20:51，跨日确认显示将新增约 3 小时 28 分钟；确认后新日显示 00:20，SQLite 保留实际模拟起点而非 09:00，随后鼠标点击悬浮菜单退出成功。见 `evidence/native-preview.json`。这是模拟验证，不是自然跨午夜。
