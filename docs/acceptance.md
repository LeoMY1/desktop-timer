# 当前阶段与验收记录

更新：2026-09-18。

## 当前状态
**用户已明确要求开始 P4。P4 v2 修复版等待用户验收；P5 未开始。**

| 阶段 | 自查/交付状态 | 用户验收状态 |
|---|---|---|
| P0 视觉 | 两张稿及浅色三版修订已交付 | 用户要求进入文档阶段，采用最新版修订 |
| P1 文档 | 文档已交付，文档一致性检查通过 | 用户明确要求“按照文档，完成P2开发”，据此进入 P2 |
| P2 原型 | v3 已交付 | 用户明确确认通过 |
| P3 计时与保存 | v1 已交付 | 用户确认睡眠/锁屏并授权进入 P4；自然午夜未实测 |
| P4 鼓励与记录 | v2 修复版应用、源码、验收入口及结果 | 待用户复测 |
| P5 最终回归 | 未实施 | 未授权 |

## P2 历史交付
- `outputs/P2-v3/StudyTimer-P2.app`：当前 Mac 原生原型。
- `outputs/P2-v3/StudyTimer-P2-v3.zip`：应用压缩包。
- `outputs/P2-v3/StudyTimer-P2-source-v3.zip`：本次源码、构建脚本、正式文档与设计参考。
- [用户测试清单](p2-testing.md)、[自测记录](p2-self-test.md)。
- `outputs/P2-v3/evidence/`：7 项几何检查、35 项 AppKit 检查报告与原生视图截图。

7/7 与 35/35 内部检查通过；真实界面已验证普通启动、状态按钮、延迟弹卡片后外部编辑焦点保留、历史中文编辑。全屏/Spaces、完整指针拖动、中文输入法候选框等仍待用户实测，详见自测记录。窗口配置检查不替代这些操作验收。

## 历史范围修正
P1 期间助手曾误将“继续下一步”理解为连续实施，提前生成的工程及代码当时已撤回，未构建或运行；当时只保留文档与设计稿。随后用户明确授权 P2，本次才建立并构建 P2 原型。该历史撤回说明不代表当前仍无源码。

## 已确认的补充交互
上方空间不足时临时下移计时窗，卡片始终在上方；关闭后恢复原位。用户主动拖动则保留新位置，仅点击不取消恢复。历史时长使用中文单位，界面不显示时区标签。

## 用户反馈与下一步
用户反馈 v1 只能从左上文字拖动，希望窗口四周均可拖动。v2 已扩展拖动范围，v3 后用户明确确认 P2 通过。收到问题后只修复 P2 并重新交付；收到明确验收通过且允许继续后，才进入 P3。不得将内部检查通过写为用户通过。

## v3 点击范围修订
用户要求右上角三个点点击范围扩大，与旁边记录图标等高。已统一为 20×20 pt 完整矩形点击区，拖动层排除该范围。实际点击三个点上方空白成功打开菜单。35 项 AppKit 回归通过，随后用户确认 P2 通过。

## P3 历史交付
- `outputs/P3-v1/StudyTimer.app` 与 `StudyTimer-P3-v1.zip`。
- `outputs/P3-v1/StudyTimer-P3-source-v1.zip`：源码、脚本、产品文档和设计参考。
- [P3 测试清单](p3-testing.md)、[自测记录](p3-self-test.md)、`outputs/P3-v1/evidence` 下原始报告。
- 26/26 隔离核心与存储检查、10/10 AppKit 集成检查、5/5 真实时间/独立进程检查通过；这些结果不等于用户验收。
- 实际睡眠/锁屏与自然跨午夜未实测，自动化使用明确标记的可控时钟补充验证。
- P3 当时不包含鼓励、备注和尾段合并；这些功能现已在 P4 交付，见下文。

## P4 v1 历史交付
应用位于 outputs/P4-v1/StudyTimer.app；同目录包含应用/源码压缩包、独立验收入口、测试单和 evidence。46/46 核心与存储检查、24/24 AppKit 集成检查通过；实际中文多行编辑和退出落盘验证通过。详见 [测试步骤](p4-testing.md) 与 [自测记录](p4-self-test.md)。

用户原文提及“P2真实睡眠、锁屏测试通过”，并明确要求开始 P4；记录为用户报告的睡眠/锁屏通过与进入 P4 授权，不冒充助手已实测，也不将自然跨午夜标为通过。

旧测试记录不导入 P4；正常退出自动处理尚未选择的尾段。两项均经本轮用户明确确认。P4 尚未收到用户验收结论，P5 未开展。

## P4 v2 用户反馈修复
用户要求星光停留 30 秒、开始闪烁 3 次，并移除退出快捷键。另报告固定 09:00 和跨日约 15 小时。只读核对发现这两类记录在旧独立验收库；正式库学习段使用实际开始时刻，未发现被模拟库覆盖。v2 将交互验收与固定自动化时钟分开，新增模拟时刻/快进提示及跨日确认，保留全部旧数据。详见 p4-v2-self-test.md；仍需用户复测，未进入 P5。
