# 桌面悬浮计时应用：开发规则

## 项目与文件
- 本文件所在目录是唯一项目根目录。根目录仅保留这一份 AGENTS.md，不建立 AGENT.md 或子目录 AGENTS.md。
- 本文件只存放长期规则。需求放 docs/requirements.md，技术规格放 docs/spec.md，分阶段交付放 docs/delivery-plan.md，当前状态与验收证据放 docs/acceptance.md。
- 开始工作前依次阅读 README.md 和以上文档，核对当前阶段及用户验收状态。

## 阶段边界：必须遵守
- 每次只执行当前获准的一个阶段。用户说“继续下一步”仅表示进入下一阶段，不代表一次授权完成后续所有阶段。
- 完成本阶段后，交付约定文件、可测试成果、具体测试步骤、自测结果和已知限制，然后停止。
- 必须等待用户明确确认该阶段验收通过并允许继续，才能进入下一阶段。不得提前编写下一阶段代码、建立其工程或运行其构建。
- 内部自测通过不等于用户验收通过；没有回复不等于通过；要求修改不等于通过。
- 用户提出本阶段问题时，只修复本阶段并重新交付。不得边等待验收边做下一阶段。
- 不修改需求以降低验收标准，不把未交付或未验证的成果标为完成。

## 协作
- 每轮最多询问 5 个问题。业务不确定或相互冲突时先确认；仅继续当前阶段内不依赖该回答的工作。
- 界面遵循用户确认的设计修订；未授权不增加业务功能。
- 不擅自创建任务、调用子代理、上传文件、引入账号或云服务。
- 报告事实、拟定方案、模拟测试和真实运行验证时明确区分。

## 实现约束（相应开发阶段获准后适用）
- 原生 Swift + SwiftUI + AppKit，最低目标 macOS 13；数据保存在本地 SQLite。
- 计时、存储与界面解耦。计时来源可注入，状态更新和持久化按顺序执行。
- 内部保留秒级精度，按 Asia/Shanghai 分日；界面不显示“北京时间”字样。
- 动画、输入、界面刷新、锁屏和睡眠不应暂停运行中的学习计时。
- 用户数据与测试、演示数据隔离；禁止为了测试覆盖真实数据库。STUDY_TIMER_DATA_DIR 作为拟定隔离配置。
- 普通悬浮控制不抢焦点；备注仅在用户主动点击时获得文本输入焦点。

## 验证与交付
- 文档阶段只检查文档，不创建应用代码、测试代码、Package.swift 或构建脚本。
- 进入开发后，测试必须与当前交付范围匹配。核心逻辑需可控时钟测试；窗口行为需真实 Mac 验证。
- 模拟睡眠或跨天测试不等于已经真实睡眠或跨零点验收。不可测场景明确写出。
- 每阶段按 docs/delivery-plan.md 给出用户能够复现的操作和预期结果。
- design 存设计稿，docs 存正式文档，work 存中间产物，outputs 存交付物；源码只在对应阶段获准后创建。
