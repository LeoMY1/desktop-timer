# desktop-timer Agent 开发规范

## 入口与文档
- 本文件是项目唯一的 AGENTS.md，保持在根目录。只写长期规则，不记录当轮进度。
- 开始任务先读 README.md、docs/requirements.md、docs/spec.md、docs/validation.md；涉及协作流程时读 docs/agent-workflow.md。
- 需求和业务规则进入 requirements，技术设计进入 spec；阶段故事与历史索引独立保存。

## 先讨论、再实现、再验收
- 开发前给出范围、实现计划、交付物和验收点，获得该阶段授权后实施。不能将“继续下一步”解释为完成所有阶段。
- 每轮最多问 5 个问题；业务规则不明确或冲突时必须确认。能从代码查明的事实先检查，不要求用户重复提供。
- 一次只做已授权阶段。完成后提供可运行成果、测试步骤、实际结果与限制，等待用户明确验收后进入下一阶段。
- 用户反馈问题时先修复当前阶段。内部测试通过、用户未回复，都不代表用户验收通过。
- 用户当前明确指令优先；已明确授权的操作不重复索要许可。不擅自委派子代理、创建任务或增加功能。

## 实现约束
- Swift、SwiftUI、AppKit、系统 SQLite；当前以 arm64、macOS 13 部署目标构建。计时、持久化与界面职责分离。
- 连续时钟覆盖睡眠；内部保留秒级精度，按 Asia/Shanghai 分日，显示时才截取分钟。
- 暂停不累计；停止不清空今日总计；正常退出保存并停止；异常恢复不补计进程关闭期间。
- 备注、动画和界面刷新不能暂停计时。普通悬浮操作不抢键盘焦点，备注在用户点击后才输入。
- 真实数据、测试数据严格隔离。测试必须使用独立 STUDY_TIMER_DATA_DIR，不删除或覆盖默认数据库，不修改系统时间。
- 不将模拟时钟或验收菜单引入日常构建；INTERNAL_TESTING 仅供内部检查构建。

## 验证、交付与开源
- 文档任务只改文档；代码任务按影响范围验证。完整回归运行 bash scripts/test.sh，日常构建运行 bash scripts/build.sh release。
- 明确区分自动化、隔离时钟、真实界面操作、用户反馈和未测项；模拟跨日、窗口标志检查不能冒充真实跨夜或全屏验收。
- 对时长和记录变更加回归检查；对低影响文案不添加镜像实现的测试。不为通过测试降低业务要求。
- work 存临时构建和测试，outputs 存本地应用与打包结果；二者不入 Git。design、docs 保存精选公开内容。
- 发布到远端前检查全部拟公开提交，不上传凭据、个人路径、真实数据库、原始聊天或个人记录。仅在用户明确授权的仓库与范围发布。
- 不伪造提交历史：历史快照导入说明来源，使用真实导入日期。清理交付物前必须验证远端可恢复。
- 说明签名、平台和未验证限制，不将可运行原型描述成已经完成跨机型验证的正式产品。
